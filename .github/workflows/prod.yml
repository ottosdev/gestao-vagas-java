name: Gestao de vagas application

on:
  push:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Setup Java 17
        uses: actions/setup-java@v3
        with:
          distribution: temurin
          java-version: 17
      - name: Build Project
        run: mvn clean install
      - name: Login Docker
        run: docker login -u ${{ secrets.DOCKER_USERNAME }} -p ${{ secrets.DOCKER_PASSWORD }}
      - name: build docker image
        run: docker build -t ${{ secrets.DOCKER_USERNAME }}/gestao-vagas .
      - name: publish image
        run: docker push ${{ secrets.DOCKER_USERNAME }}/gestao-vagas
  deploy:
    needs: build
    runs-on: self-hosted
    steps:
      - name: pull image docker hub
        run: docker pull ${{ secrets.DOCKER_USERNAME }}/gestao-vagas
      - name: Remove container
        run: docker rm -f gestao-vagas
      - name: run docker container
        run: docker run -d -p 8080:8080 -e DATABASE_URL=${{secrets.DATABASE_URL}} -e DATABASE_PASSWORD=${{secrets.DATABASE_PASSWORD}} -e  ${{ secrets.DATABASE_USERNAME }} --name gestao-vagas ${{ secrets.DOCKER_USERNAME }}/gestao-vagas
        with:
          host: ${{ secrets.HOST }}
          username: ${{ secrets.USERNAME }}
          key: ${{ secrets.KEY }}
          script: |